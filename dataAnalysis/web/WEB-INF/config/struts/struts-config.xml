<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE struts-config PUBLIC "-//Apache Software Foundation//DTD Struts Configuration 1.2//EN" "http://jakarta.apache.org/struts/dtds/struts-config_1_2.dtd">

<struts-config>

  <!-- ========== Data Sources Definitions =================================== -->
  <!--
    Define your Struts data sources in a file called struts-data-sources.xml and place
    it in your merge directory.
  -->

  <!-- ========== Global Exceptions Definitions =================================== -->
      <global-exceptions>
        <exception type="java.lang.Exception"
            key="errors.general"
            handler="com.liusy.web.action.ActionExceptionHandler"/>
    </global-exceptions>

  <!-- ========== Global Forward Definitions =================================== -->
      <global-forwards>		
        <forward name="logout" path="/logout.jsp" redirect="true" />
        <forward name="noPrivilege" path="/common/noPrivilege.jsp"/>
        <forward name="cancel" path="/common/main.jsp" />
         <forward name="error" path="/common/error.jsp"></forward>
         <forward name="message" path="/common/message.jsp"></forward>
         <forward name="commmsg" path="/common/commmessage.jsp"></forward>
         <forward name="relogin" path="/common/relogin.jsp"></forward>
    </global-forwards>

  <controller nocache="true" inputForward="true" maxFileSize="2M"/>

      <message-resources parameter="ApplicationResources"/>

      <!-- ========== Validator plugin ===================  
    <plug-in className="org.apache.struts.validator.ValidatorPlugIn">
        <set-property property="pathnames" 
                      value="/WEB-INF/validator-rules.xml,
                             /WEB-INF/validator-rules-custom.xml,
                             /WEB-INF/validation.xml"/>
    </plug-in>
	-->

</struts-config>
